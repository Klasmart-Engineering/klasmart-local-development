apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: auth-frontend-deployment
spec:
  hosts:
    - '*'
  gateways:
    - local-gateway
  http:
    - route:
        - destination:
            host: auth-frontend-deployment-svc
            port:
              number: 80
